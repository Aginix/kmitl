<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- View purchase.order form -->
    <record id="view_purchase_order_form" model="ir.ui.view">
        <field name="name">view.purchase.order.form</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Budget" name="budget_page">
                    <group
                        name="budget_details" string="Budget Details"
                        attrs="{'readonly': [('state', 'in', ('purchase', 'done', 'cancel'))]}"
                    >
                        <field name="product_id" invisible="1" />
                        <field
                            name="analytic_distribution"
                            widget="analytic_distribution"
                            invisible="1"
                        />
                        <field
                            name="analytic_distribution"
                            widget="analytic_distribution"
                            invisible="0"
                            groups="base.group_erp_manager"
                        />
                        <field name="budget_account_id"/>
                        <field name="activity_analytic_id" string="Activity"/>
                        <field name="department_analytic_id" string="Department"/>
                        <field name="fund_analytic_id" string="Fund"/>
                        <field name="source_analytic_id" string="Source"/>
                        <field name="procurement_plan_analytic_id" invisible="1" />
                    </group>
                    <group name="procurement_plan" string="Procurement Plan">
                        <field
                            name="use_procurement_plan"
                            attrs="{'readonly': [('state', 'in', ('purchase', 'done', 'cancel'))]}"
                        />
                        <field name="account_fiscal_year_id" invisible="1" />
                        <field
                            name="procurement_plan_id"
                            options="{'no_create_edit': True, 'no_open': True}"
                            domain="[('state', '=', 'pending'), ('account_fiscal_year_id', '=', account_fiscal_year_id)]"
                            attrs="{'invisible': [('use_procurement_plan', '=', False)], 'required': [('use_procurement_plan', '=', True)], 'readonly': [('state', 'in', ('purchase', 'done', 'cancel'))]}"
                        />
                    </group>
                </page>
            </xpath>
            <xpath expr="//div[hasclass('oe_button_box')]" position="inside">
                <button
                    name="action_open_budget_commitment"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-money"
                    attrs="{'invisible':[('budget_commitment_id','=',False)]}"
                >
                    <field name="budget_commitment_id" invisible="1" />
                    <div class="o_stat_info">
                        <span>Budget Commitment</span>
                    </div>
                </button>
            </xpath>
            <xpath expr="//div[hasclass('oe_button_box')]" position="inside">
                <button
                    name="action_view_procurement_plan"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-list-alt"
                    attrs="{'invisible': [('procurement_plan_id', '=', False)]}"
                >
                    <div class="o_field_widget o_stat_info">
                        <span class="o_stat_text">Procurement Plan</span>
                    </div>
                </button>
            </xpath>
            <xpath
                expr="//field[@name='order_line']/tree/field[@name='analytic_distribution']"
                position="attributes"
            >
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath
                expr="//field[@name='order_line']/tree/field[@name='analytic_distribution']"
                position="after"
            >
                <field
                    name="analytic_distribution"
                    widget="analytic_distribution"
                    invisible="0"
                    optional="hide"
                    groups="base.group_erp_manager"
                />
            </xpath>
            <xpath
                expr="//field[@name='order_line']//tree/field[@name='product_id']"
                position="attributes"
            >
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='order_line']" position="attributes">
                <attribute name="context">{'default_state': 'draft', 'default_product_id': product_id}</attribute>
            </xpath>
        </field>
    </record>
</odoo>
