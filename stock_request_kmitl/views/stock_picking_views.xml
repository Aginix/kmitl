<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- View stock.picking form -->
    <record id="view_stock_picking_form" model="ir.ui.view">
        <field name="name">view.stock.picking.form</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='origin']" position="after">
                <field name="has_stock_request" invisible="1"/>
            </xpath>
            <xpath expr="//div[@name='button_box']" position="inside">
                <button
                    name="action_view_stock_request"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-cubes"
                    attrs="{'invisible': [('has_stock_request', '=', False)]}"
                >
                    <div class="o_stat_info">
                        <span class="o_stat_text">Stock Request</span>
                    </div>
                </button>
            </xpath>
        </field>
    </record>
</odoo>
