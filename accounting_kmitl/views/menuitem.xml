<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Menu-scoped search views: searchpanel ประเภทคู่ค้า (partner_type_id).
         Primary inherits so the base account.move search view stays untouched — only
         the menus whose actions bind these views expose the panel.
         account.view_account_move_filter has NO searchpanel → create one. -->
    <record id="view_account_move_filter_vendor_bills_kmitl" model="ir.ui.view">
        <field name="name">account.move.filter.vendor.bills.kmitl</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_account_move_filter"/>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <xpath expr="//search" position="inside">
                <searchpanel>
                    <field name="partner_type_id" string="ประเภทคู่ค้า"
                           icon="fa-user" select="multi" enable_counters="1"/>
                </searchpanel>
            </xpath>
        </field>
    </record>

    <record id="view_account_move_filter_vendor_payment_moves_kmitl" model="ir.ui.view">
        <field name="name">account.move.filter.vendor.payment.moves.kmitl</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_account_move_filter"/>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <xpath expr="//search" position="inside">
                <searchpanel>
                    <field name="partner_type_id" string="ประเภทคู่ค้า"
                           icon="fa-user" select="multi" enable_counters="1"/>
                </searchpanel>
            </xpath>
        </field>
    </record>

    <record id="view_account_move_filter_account_entries_kmitl" model="ir.ui.view">
        <field name="name">account.move.filter.account.entries.kmitl</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_account_move_filter"/>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <xpath expr="//search" position="inside">
                <searchpanel>
                    <field name="partner_type_id" string="ประเภทคู่ค้า"
                           icon="fa-user" select="multi" enable_counters="1"/>
                </searchpanel>
            </xpath>
        </field>
    </record>

    <!-- Bind menus to their scoped search views (must come after view definitions above). -->
    <record id="account.action_move_in_invoice_type" model="ir.actions.act_window">
        <field name="search_view_id" ref="view_account_move_filter_vendor_bills_kmitl"/>
    </record>
    <record id="action_account_entries" model="ir.actions.act_window">
        <field name="search_view_id" ref="view_account_move_filter_account_entries_kmitl"/>
    </record>

    <!-- Root menu -->
    <menuitem
        id="menu_accounting_root"
        name="Accounting"
        web_icon="accounting_kmitl,static/description/icon.png"
        sequence="7"
        groups="accounting_kmitl.group_accounting_kmitl_user"
    />

    <!-- Dashboard (landing page — lowest sequence so it opens first) -->
    <menuitem
        id="menu_accounting_dashboard"
        name="Dashboard"
        parent="menu_accounting_root"
        action="action_accounting_dashboard"
        sequence="5"
    />

    <!-- เจ้าหนี้ (Accounts Payable) -->
    <menuitem
        id="menu_payable"
        name="เจ้าหนี้"
        parent="menu_accounting_root"
        sequence="10"
    />
    <menuitem
        id="menu_vendor_bills"
        name="ใบตั้งหนี้"
        parent="menu_payable"
        action="account.action_move_in_invoice_type"
        sequence="10"
    />

    <!-- ลูกหนี้ (Accounts Receivable) -->
    <menuitem
        id="menu_receivable"
        name="ลูกหนี้"
        parent="menu_accounting_root"
        sequence="11"
    />
    <menuitem
        id="menu_receivable_bills"
        name="ใบตั้งหนี้"
        parent="menu_receivable"
        action="account.action_move_out_invoice_type"
        sequence="10"
    />

    <!-- ใบล้างเจ้าหนี้ — journal entries created from vendor payments -->
    <record id="action_vendor_payment_moves" model="ir.actions.act_window">
        <field name="name">ใบล้างเจ้าหนี้</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('payment_id', '!=', False), ('payment_id.partner_type', '=', 'supplier'), ('payment_id.payment_type', '=', 'outbound')]</field>
        <field name="context">{'create': False}</field>
        <field name="search_view_id" ref="view_account_move_filter_vendor_payment_moves_kmitl"/>
    </record>
    <menuitem
        id="menu_vendor_payment_moves"
        name="ใบล้างเจ้าหนี้"
        parent="menu_payable"
        action="action_vendor_payment_moves"
        sequence="20"
    />

    <!-- Journal Entries -->
    <menuitem
        id="menu_journal_entries"
        name="Journal Entries"
        parent="menu_accounting_root"
        action="account.action_move_journal_line"
        sequence="20"
    />

    <!-- All account.move entries (รายการเดินบัญชี) under Journal Entries -->
    <menuitem
        id="menu_account_entries"
        name="Account Entries"
        parent="menu_journal_entries"
        action="action_account_entries"
        sequence="5"
    />

    <!-- All Journal Items (account.move.line) under Journal Entries -->
    <record id="action_account_move_lines_all" model="ir.actions.act_window">
        <field name="name">Journal Items</field>
        <field name="res_model">account.move.line</field>
        <field name="view_mode">tree,pivot,graph,kanban,form</field>
        <field name="view_id" ref="view_move_line_tree_debit_first"/>
        <field name="search_view_id" ref="account.view_account_move_line_filter"/>
        <field name="domain">[('display_type', 'not in', ('line_section', 'line_note'))]</field>
        <field name="context">{'search_default_group_by_move': 1, 'create': 0}</field>
    </record>
    <menuitem
        id="menu_journal_items_all"
        name="Journal Items"
        parent="menu_journal_entries"
        action="action_account_move_lines_all"
        sequence="10"
    />

    <!-- Reports submenu (placeholder — extend ผ่านโมดูล report ในอนาคต) -->
    <menuitem
        id="menu_accounting_reports"
        name="Reports"
        parent="menu_accounting_root"
        sequence="80"
    />

    <!-- Configuration (manager only) -->
    <menuitem
        id="menu_accounting_config"
        name="Configuration"
        parent="menu_accounting_root"
        sequence="90"
        groups="accounting_kmitl.group_accounting_kmitl_manager"
    />
    <menuitem
        id="menu_config_chart_of_accounts"
        name="Chart of Accounts"
        parent="menu_accounting_config"
        action="account.action_account_form"
        sequence="10"
    />
    <menuitem
        id="menu_config_journals"
        name="Journals"
        parent="menu_accounting_config"
        action="account.action_account_journal_form"
        sequence="20"
    />
    <menuitem
        id="menu_config_taxes"
        name="Taxes"
        parent="menu_accounting_config"
        action="account.action_tax_form"
        sequence="30"
    />
    <menuitem
        id="menu_config_payment_methods"
        name="Payment Methods"
        parent="menu_accounting_config"
        action="action_account_payment_method"
        sequence="40"
    />
    <menuitem
        id="menu_config_payment_method_lines"
        name="Payment Method Lines"
        parent="menu_accounting_config"
        action="action_account_payment_method_line"
        sequence="50"
    />

</odoo>
