<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_purchase_request_line_make_purchase_order_approval_inherit" model="ir.ui.view">
        <field name="name">Purchase Request Line Make Purchase Order (Approval)</field>
        <field name="model">purchase.request.line.make.purchase.order</field>
        <field name="inherit_id" ref="purchase_request.view_purchase_request_line_make_purchase_order" />
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <field name="estimated_cost" invisible="1" />
            </xpath>
            <xpath expr="//field[@name='item_ids']" position="attributes">
                <attribute name="attrs">{'invisible': [('estimated_cost', '&lt;=', 100000)]}</attribute>
            </xpath>
            <xpath expr="//field[@name='supplier_id']" position="attributes">
                <attribute name="attrs">{'invisible': [('estimated_cost', '&lt;=', 100000)]}</attribute>
            </xpath>
            <!-- <xpath expr="//field[@name='supplier_id']" position="after">
                <field name="supplier_id" readonly="1" options="{'no_open': True}" />
                <field name="estimated_cost" readonly="1" />
            </xpath> -->
            <!-- <form string="Create RFQ">
                <separator string="Existing RFQ to update:" />
                <newline />
                <group>
                    <field name="purchase_order_id" domain="[('partner_id', '=', supplier_id)]" />
                    <field name="sync_data_planned" />
                </group>
                <newline />
                <separator string="New PO details:" />
                <newline />
                <group>
                    <field name="supplier_id" />
                </group>
                <newline />
                <group>
                    <field name="item_ids" nolabel="1" colspan="2">
                        <tree name="Details" create="false" editable="bottom">
                            <field name="line_id" options="{'no_open': true}" invisible="1" />
                            <field name="request_id" attrs="{'readonly': True}" />
                            <field name="product_id" />
                            <field name="name" />
                            <field name="estimated_cost" />
                            <field name="product_qty" />
                            <field name="product_uom_id" invisible="1" groups="!uom.group_uom" />
                            <field name="product_uom_id" groups="uom.group_uom" />
                            <field name="keep_description" widget="boolean_toggle" options="{'autosave': False}" />
                            <field name="keep_estimated_cost" widget="boolean_toggle" options="{'autosave': False}" />
                        </tree>
                    </field>
                </group>
                <newline />
                <footer>
                    <button name="make_purchase_order" string="Create RFQ" type="object" class="oe_highlight" />
                    <button special="cancel" string="Cancel" class="oe_link" />
                </footer>
            </form> -->
        </field>
    </record>
</odoo>
