<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_purchase_request_approval_line_tree" model="ir.ui.view">
        <field name="name">view.purchase.request.approval.line.tree</field>
        <field name="model">purchase.request.approval.line</field>
        <field name="arch" type="xml">
            <tree>
                <field name="product_id"/>
                <field name="description"/>
            </tree>
        </field>
    </record>

    <record id="view_purchase_request_approval_line_form" model="ir.ui.view">
        <field name="name">view.purchase.request.approval.line.form</field>
        <field name="model">purchase.request.approval.line</field>
        <field name="arch" type="xml">
            <form string="Purchase Request Approval Line">
                <sheet>
                    <group>
                        <group>
                            <field name="product_id" />
                            <field name="description"/>
                        </group>
                        <group>
                            <field name="product_qty"/>
                            <field name="uom"/>
                            <field name="taxes_id" widget="many2many_tags" />
                            <field name="price_unit"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_purchase_request_approval_line" model="ir.actions.act_window">
        <field name="name">Purchase Request Approval Line</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">purchase.request.approval.line</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Purchase Request Approval Line.
            </p>
        </field>
    </record>

</odoo>
