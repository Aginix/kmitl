<?xml version="1.0" encoding="utf-8"?>
  <odoo>
    <record id="view_purchase_request_report_form" model="ir.ui.view">
        <field name="name">view.purchase.request.report.form</field>
        <field name="model">purchase.request.report</field>
        <field name="inherit_id" ref="purchase_approval.view_purchase_request_report_form"/>
        <field name="arch" type="xml">
            <field name="state" position="after">
                <field name="validation_status" invisible="1" />
            </field>
        </field>
    </record>

    <record id="view_purchase_approval_filter" model="ir.ui.view">
        <field
            name="name"
        >purchase.approval.select - purchase_approval_tier_validation</field>
        <field name="model">purchase.request.report</field>
        <field name="inherit_id" ref="purchase_approval.view_purchase_request_report_search" />
        <field name="arch" type="xml">
            <search position="inside">
                <group expand="0" string="Need actions">
                    <filter
                        name="needs_review"
                        string="Needs my Review"
                        domain="[('reviewer_ids','in',uid), ('state', 'not in', ['done', 'cancel'])]"
                        help="My Purchases to review"
                    />
                </group>
            </search>
        </field>
    </record>
  </odoo>
