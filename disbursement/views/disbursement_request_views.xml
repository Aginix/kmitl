<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Tree View -->
    <record id="view_disbursement_request_tree" model="ir.ui.view">
        <field name="name">disbursement.request.tree</field>
        <field name="model">disbursement.request</field>
        <field name="arch" type="xml">
            <tree string="Disbursement Requests">
                <field name="name" />
                <field name="partner_id" />
                <field name="user_id" optional="show" />
                <field name="date" />
                <field name="account_fiscal_year_id" options="{'no_open': True}" />
                <field name="payment_type" />
                <field name="ref" />
                <field name="amount_total" />
                <field name="state" widget="badge" decoration-info="state == 'draft'" decoration-warning="state in ('submitted', 'sent')" decoration-primary="state in ('validated', 'waiting_bill_post', 'bill_posted', 'waiting_payment_post')" decoration-success="state in ('approved', 'payment_posted', 'done')" />
                <field name="main_exception_id" />
                <field name="budget_commitment_id" optional="hide" />
                <field name="source_analytic_id" optional="show" />
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_disbursement_request_form" model="ir.ui.view">
        <field name="name">disbursement.request.form</field>
        <field name="model">disbursement.request</field>
        <field name="arch" type="xml">
            <form string="Disbursement Request">
                <header>
                    <button name="action_submit" string="Submit" type="object" class="oe_highlight" attrs="{'invisible': [('state', '!=', 'draft')]}" />
                    <button name="action_send" string="Send" type="object" class="oe_highlight" attrs="{'invisible': [('state', '!=', 'submitted')]}" groups="disbursement.group_disbursement_head" />
                    <button name="action_validate" string="Validate" type="object" class="oe_highlight" attrs="{'invisible': [('state', '!=', 'sent')]}" confirm="Are you sure you want to validate this request?" groups="disbursement.group_disbursement_officer" />
                    <button name="action_approve" string="Approve" type="object" class="oe_highlight" attrs="{'invisible': [('state', '!=', 'validated')]}" confirm="Are you sure you want to approve? This will reserve budget." groups="disbursement.group_disbursement_manager" />
                    <button name="action_create_bill" string="Create Bill" type="object" class="oe_highlight" attrs="{'invisible': ['|', ('state', '!=', 'approved'), ('bill_count', '>', 0)]}" groups="disbursement.group_disbursement_officer" />
                    <button name="action_register_payment"
                            string="Register Payment"
                            type="object"
                            class="oe_highlight"
                            attrs="{'invisible': ['|', ('state', 'not in', ('bill_posted', 'waiting_payment_post', 'payment_posted')), ('hide_register_payment_button', '=', True)]}"
                            groups="account.group_account_invoice"/>
                    <button name="action_draft" string="Reset to draft" type="object" class="btn btn-secondary" attrs="{'invisible': [('state', 'not in', ('submitted', 'sent', 'cancel'))]}" groups="disbursement.group_disbursement_manager" />
                    <button name="action_cancel" string="Cancel" type="object" attrs="{'invisible': [('state', 'in', ('draft', 'done', 'cancel', 'bill_posted', 'waiting_payment_post', 'payment_posted'))]}" groups="disbursement.group_disbursement_officer" />
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,sent,validated,approved,waiting_bill_post,bill_posted,waiting_payment_post,payment_posted,done" />
                </header>
                <!-- Exception Alert -->
                <div class="alert alert-danger" role="alert" style="margin-bottom:0px;" attrs="{'invisible': [('exceptions_summary','=',False)]}">
                    <p>
                        <strong>There are exceptions blocking this Disbursement Request:</strong>
                    </p>
                    <field name="exceptions_summary" />
                    <button name="action_ignore_exceptions" type="object" class="btn-danger" string="Ignore Exceptions" help="Click here to be able to confirm this Disbursement Request regardless of the exceptions." groups="base_exception.group_exception_rule_manager" />
                </div>
                <sheet>
                    <field name="hide_register_payment_button" invisible="1"/>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_bill" type="object" class="oe_stat_button" icon="fa-pencil-square-o" attrs="{'invisible': [('bill_count', '=', 0)]}">
                            <field name="bill_count" widget="statinfo" string="Bill" />
                        </button>
                        <button name="action_view_payments"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-money"
                                attrs="{'invisible': [('payment_count', '=', 0)]}">
                            <field name="payment_count" widget="statinfo" string="Payments"/>
                        </button>
                        <button name="open_preview" type="object" class="oe_stat_button" icon="fa-globe">
                            <div class="o_field_widget o_stat_info">
                                <span class="o_stat_text">Preview</span>
                            </div>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1" />
                        </h1>
                    </div>
                    <group>
                        <group id="header_left_group" string="ข้อมูลคู่ค้า">
                            <field name="partner_id" widget="res_partner_many2one" context="{'show_address': 1, 'default_is_company': True, 'show_vat': True}" options='{"always_reload": True}' />
                            <field name="partner_bank_id" context="{'default_partner_id': partner_id}" domain="[('partner_id', '=', partner_id)]" />
                            <field name="company_id" groups="base.group_multi_company" />
                            <field name="is_company" readonly="1" invisible="1" />
                            <field name="ignore_exception" states="to_approve" groups='base_exception.group_exception_rule_manager' />
                            <field name="exception_ids" widget="many2many_tags" readonly="True" groups='base.group_no_one' />
                        </group>
                        <group id="header_right_group" string="ข้อมูลการเบิก">
                            <field name="date" />
                            <field name="reference_model_name" readonly="1"
                                attrs="{'invisible': ['|', ('id', '=', False), ('reference_model_name', '=', False)]}" />
                            <field name="reference"
                                attrs="{'readonly': ['|', '|', ('id', '!=', False), ('state', '!=', 'draft'), ('reference_model', '!=', False)]}"
                                force_save="1" />
                            <field name="reference_model" invisible="1" />
                            <field name="ref" />
                            <field name="payment_type" required="1" />
                        </group>
                        <group id="budget" string="มิติทางบัญชี">
                            <field name="account_fiscal_year_id" readonly="1" options="{'no_open': True}" />
                            <field name="budget_commitment_id" />
                            <field name="source_analytic_id" required="1" options="{'no_create': True, 'no_open': True}" attrs="{'readonly': ['|', ('budget_commitment_id', '!=', False), ('state', '!=', 'draft')]}" />
                            <field name="department_analytic_id" required="1" options="{'no_create': True, 'no_open': True}" attrs="{'readonly': ['|', ('budget_commitment_id', '!=', False), ('state', '!=', 'draft')]}" />
                            <field name="fund_analytic_id" required="1" options="{'no_create': True, 'no_open': True}" attrs="{'readonly': ['|', ('budget_commitment_id', '!=', False), ('state', '!=', 'draft')]}" />
                            <field name="activity_analytic_id" required="1" options="{'no_create': True, 'no_open': True}" attrs="{'readonly': ['|', ('budget_commitment_id', '!=', False), ('state', '!=', 'draft')]}" />
                            <field name="budget_account_id" options="{'no_create': True, 'no_open': True}" attrs="{'readonly': ['|', ('budget_commitment_id', '!=', False), ('state', '!=', 'draft')]}" />
                            <field name="budget_account_id" invisible="1" />
                            <field name="analytic_distribution" invisible="1"/>
                        </group>
                        <group id="other_information" string="ข้อมูลอื่น ๆ">
                            <field name="user_id" groups="disbursement.group_disbursement_manager" />
                            <field name="user_id" readonly="1" groups="!disbursement.group_disbursement_manager" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Request Lines" name="lines">
                            <field name="line_ids" widget="one2many" context="{'default_request_id': active_id}">
                                <tree editable="bottom">
                                    <field name="sequence" widget="handle" />
                                    <field name="product_id" />
                                    <field name="name" />
                                    <field name="quantity" />
                                    <field name="price_unit" />
                                    <field name="tax_ids" widget="many2many_tags" />
                                    <field name="wht_tax_id" optional="show"/>
                                    <field name="account_id" />
                                    <field name="analytic_distribution" widget="analytic_distribution" groups="analytic.group_analytic_accounting" readonly="1" />
                                    <field name="price_subtotal" />
                                    <field name="price_tax" optional="hide" />
                                    <field name="price_total" />
                                    <field name="company_id" invisible="1" />
                                    <field name="currency_id" invisible="1" />
                                </tree>
                            </field>
                            <group class="oe_subtotal_footer oe_right">
                                <field name="amount_untaxed" />
                                <field name="amount_tax" />
                                <field name="amount_total" />
                                <field name="amount_wht" />
                                <field name="amount_net" class="oe_subtotal_footer_separator" />
                            </group>
                            <div class="oe_clear" />
                        </page>
                        <page string="Pipeline" name="pipeline"
                            attrs="{'invisible': [('state', 'not in', ['waiting_bill_post', 'bill_posted', 'waiting_payment_post', 'payment_posted', 'done'])]}">
                            <group>
                                <group string="Vendor Bill">
                                    <field name="bill_id" readonly="1"/>
                                    <field name="bill_payment_state" widget="badge"
                                        decoration-danger="bill_payment_state == 'not_paid'"
                                        decoration-warning="bill_payment_state == 'partial'"
                                        decoration-success="bill_payment_state == 'paid'"
                                        attrs="{'invisible': [('bill_id', '=', False)]}"/>
                                    <field name="bill_amount_residual"
                                        attrs="{'invisible': [('bill_id', '=', False)]}"/>
                                </group>
                                <group string="Payments">
                                    <field name="payment_ids" widget="many2many_tags"
                                        attrs="{'invisible': [('payment_count', '=', 0)]}"/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" />
                    <field name="activity_ids" />
                    <field name="message_ids" />
                </div>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_disbursement_request_search" model="ir.ui.view">
        <field name="name">disbursement.request.search</field>
        <field name="model">disbursement.request</field>
        <field name="arch" type="xml">
            <search string="Disbursement Requests">
                <field name="name" string="Name" filter_domain="['|', ('name', 'ilike', self), ('ref', 'ilike', self)]" />
                <field name="partner_id" />
                <field name="account_fiscal_year_id" />
                <field name="source_analytic_id" domain="[('root_plan_id.code', '=', 'sources')]" />
                <separator />
                <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]" />
                <filter string="Submitted" name="submitted" domain="[('state', '=', 'submitted')]" />
                <filter string="Sent" name="sent" domain="[('state', '=', 'sent')]" />
                <filter string="Validated" name="validated" domain="[('state', '=', 'validated')]" />
                <filter string="Approved" name="approved" domain="[('state', '=', 'approved')]" />
                <filter string="Waiting Bill Post" name="waiting_bill_post" domain="[('state', '=', 'waiting_bill_post')]" />
                <filter string="Bill Posted" name="bill_posted" domain="[('state', '=', 'bill_posted')]" />
                <filter string="Waiting Payment Post" name="waiting_payment_post" domain="[('state', '=', 'waiting_payment_post')]" />
                <filter string="Payment Posted" name="payment_posted" domain="[('state', '=', 'payment_posted')]" />
                <filter string="Done" name="done" domain="[('state', '=', 'done')]" />
                <separator />
                <filter icon="fa-exclamation-circle" name="tofix" string="Blocked in Draft" domain="[('main_exception_id','!=',False)]" />
                <separator />
                <filter name="date" string="Date" date="date" />
                <group expand="0" string="Group By">
                    <filter string="Partner" name="partner" context="{'group_by': 'partner_id'}" />
                    <filter string="Date" name="date" context="{'group_by': 'date'}" />
                    <filter string="State" name="state" context="{'group_by': 'state'}" />
                    <filter string="Payment Type" name="payment_type" context="{'group_by': 'payment_type'}" />

                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_disbursement_request" model="ir.actions.act_window">
        <field name="name">Disbursement Requests</field>
        <field name="res_model">disbursement.request</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new Disbursement Request
            </p>
            <p>
                Disbursement requests are used to hold information before creating bills.
            </p>
        </field>
    </record>

    <!-- Root menu for Disbursement app -->
    <menuitem id="menu_disbursement_root" name="Disbursement" sequence="45" web_icon="disbursement,static/description/icon.png"/>

    <!-- Disbursement Requests submenu -->
    <menuitem id="menu_disbursement_request" name="Disbursement Requests" parent="menu_disbursement_root" action="action_disbursement_request" sequence="10" groups="disbursement.group_disbursement_user"/>

    <!-- Configuration submenu -->
    <menuitem id="menu_disbursement_config" name="Configuration" parent="menu_disbursement_root" sequence="100" groups="disbursement.group_disbursement_manager"/>
</odoo>
