<?xml version="1.0" encoding="UTF-8" ?>
<odoo noupdate="1">
    <!-- UC3: notify the requester when the PR (พ.1) changes status (approved/rejected) -->
    <record id="mail_activity_pr_status_fyi" model="mail.activity.type">
        <field name="name">Purchase request status update</field>
        <field name="summary">The purchase request changed status</field>
        <field name="todo_category">acknowledgement</field>
        <field name="res_model">purchase.request.approval</field>
        <field name="icon">fa-info-circle</field>
        <field name="delay_count">0</field>
    </record>

    <!-- Execution Todo: reserve the budget commitment while the พ.1 sits at to_verify (รอจองงบประมาณ) -->
    <record id="mail_activity_pr_reserve_budget" model="mail.activity.type">
        <field name="name">Reserve budget (พ.1)</field>
        <field
            name="summary"
        >Reserve the budget commitment for the purchase request</field>
        <field name="todo_category">execution</field>
        <field name="res_model">purchase.request</field>
        <field name="icon">fa-money</field>
        <field name="delay_count">0</field>
    </record>

    <!-- Execution Todo: create the endorsement letter (หนังสือขอความเห็นชอบให้จัดหา) at to_submit -->
    <record
        id="mail_activity_pr_awaiting_endorsement_letter"
        model="mail.activity.type"
    >
        <field name="name">Awaiting endorsement letter (พ.1)</field>
        <field
            name="summary"
        >Create the endorsement letter for the purchase request</field>
        <field name="todo_category">execution</field>
        <field name="res_model">purchase.request</field>
        <field name="icon">fa-file-text-o</field>
        <field name="delay_count">0</field>
    </record>

    <!-- Execution Todo: authorized signer must sign the circulating Sarabun document at to_approve -->
    <record id="mail_activity_pr_awaiting_signer" model="mail.activity.type">
        <field name="name">Awaiting authorized signer (พ.1)</field>
        <field name="summary">Sign the endorsement letter for the purchase request</field>
        <field name="todo_category">execution</field>
        <field name="res_model">purchase.request</field>
        <field name="icon">fa-pencil-square-o</field>
        <field name="delay_count">0</field>
    </record>

    <!-- Execution Todo: PR endorsed — proceed with PA creation (non-EGP) or EGP recording -->
    <record id="mail_activity_pr_endorsement_approved" model="mail.activity.type">
        <field name="name">Endorsement approved — proceed (พ.1)</field>
        <field
            name="summary"
        >Purchase request endorsement approved — proceed with next step</field>
        <field name="todo_category">execution</field>
        <field name="res_model">purchase.request</field>
        <field name="icon">fa-check-circle</field>
        <field name="delay_count">0</field>
    </record>

    <!-- Execution Todo: PA (พจ.1) submitted for approval — manager consideration -->
    <record id="mail_activity_pa_awaiting_manager" model="mail.activity.type">
        <field name="name">Awaiting manager consideration (พจ.1)</field>
        <field name="summary">Consider the purchase request approval (พจ.1)</field>
        <field name="todo_category">execution</field>
        <field name="res_model">purchase.request.approval</field>
        <field name="icon">fa-gavel</field>
        <field name="delay_count">0</field>
    </record>

    <!-- Execution Todo: PA approved — record contract / purchase order data -->
    <record
        id="mail_activity_pa_approved_record_contract"
        model="mail.activity.type"
    >
        <field name="name">Record contract data (พจ.1 approved)</field>
        <field
            name="summary"
        >Record contract / purchase order data after PA approval</field>
        <field name="todo_category">execution</field>
        <field name="res_model">purchase.request.approval</field>
        <field name="icon">fa-file-text</field>
        <field name="delay_count">0</field>
    </record>

    <!-- Execution Todo: EGP path — record the e-GP project number so procurement can proceed -->
    <record id="mail_activity_pr_egp_number_entry" model="mail.activity.type">
        <field name="name">Enter e-GP project number (พ.1)</field>
        <field name="summary">Enter the e-GP project number to proceed with procurement</field>
        <field name="todo_category">execution</field>
        <field name="res_model">purchase.request</field>
        <field name="icon">fa-globe</field>
        <field name="delay_count">0</field>
    </record>
</odoo>
