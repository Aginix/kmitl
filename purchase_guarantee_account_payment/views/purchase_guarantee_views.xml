<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_purchase_guarantee_form" model="ir.ui.view">
        <field name="name">purchase.guarantee.form.account.payment</field>
        <field name="model">purchase.guarantee</field>
        <field
            name="inherit_id"
            ref="l10n_th_gov_purchase_guarantee.view_purchase_guarantee_form"
        />
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="before">
                <header>
                    <button
                        name="action_create_payment"
                        string="สร้างใบเบิกจ่าย"
                        type="object"
                        class="btn-primary"
                    />
                </header>
            </xpath>
            <xpath expr="//div[@name='button_box']" position="inside">
                <button
                    name="action_view_payments"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-money"
                    attrs="{'invisible': [('payment_count', '=', 0)]}"
                >
                    <field
                        name="payment_count"
                        widget="statinfo"
                        string="Payments"
                    />
                </button>
            </xpath>
        </field>
    </record>
</odoo>
