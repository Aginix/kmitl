<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <record id="pr_exception_procurement_plan_1" model="exception.rule">
        <field name="name">The value of the procurement request cannot exceed the procurement plan.</field>
        <field name="description">Procurement Plan</field>
        <field name="model">purchase.request</field>
        <field name="exception_type">by_py_code</field>
        <field name="code"><![CDATA[
if self.use_procurement_plan and self.procurement_plan_id and self.estimated_cost > self.procurement_plan_id.total_price:
    failed = True
]]>
        </field>
    </record>

    <record id="pr_exception_procurement_plan_budget" model="exception.rule">
        <field name="name">The budget allocation information does not match the procurement plan.</field>
        <field name="description">Procurement Plan</field>
        <field name="model">purchase.request</field>
        <field name="exception_type">by_py_code</field>
        <field name="code"><![CDATA[
if self.procurement_plan_id:
    data = [
        (self.account_fiscal_year_id.id, self.procurement_plan_id.account_fiscal_year_id.id),
        (self.budget_account_id.id, self.procurement_plan_id.budget_account_id.id),
        (set(self.analytic_distribution.keys()), set(self.procurement_plan_id.analytic_distribution.keys()))
    ]
    for val, plan_val in data:
        if val != plan_val:
            failed = True
            continue
]]>
        </field>
    </record>
</odoo>
