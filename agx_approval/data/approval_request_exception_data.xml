<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="excep_zero_amount_line" model="exception.rule">
            <field name="name">มีรายการค่าใช้จ่ายที่จำนวนเงินเป็น 0</field>
            <field name="description">ทุกรายการค่าใช้จ่าย (แผน) ต้องระบุจำนวนเงินมากกว่า 0</field>
            <field name="sequence">10</field>
            <field name="model">approval.request</field>
            <field name="exception_type">by_py_code</field>
            <field name="code">
if any(l.total_amount &lt;= 0 for l in self.line_ids):
    failed = True
            </field>
            <field name="is_blocking" eval="True"/>
            <field name="active" eval="True"/>
        </record>

        <record id="excep_no_participant" model="exception.rule">
            <field name="name">ไม่มีรายชื่อผู้เกี่ยวข้อง</field>
            <field name="description">คำขอต้องมีรายชื่ออย่างน้อย 1 รายชื่อ</field>
            <field name="sequence">20</field>
            <field name="model">approval.request</field>
            <field name="exception_type">by_py_code</field>
            <field name="code">
if not self.participant_ids:
    failed = True
            </field>
            <field name="is_blocking" eval="True"/>
            <field name="active" eval="True"/>
        </record>

    </data>
</odoo>
