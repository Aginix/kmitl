<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="0">
    <record id="pr_line_exception_name_required" model="exception.rule">
        <field name="name">ต้องกรอกชื่อรายการ</field>
        <field name="description">ชื่อรายการห้ามปล่อยว่าง</field>
        <field name="model">purchase.request.line</field>
        <field name="exception_type">by_py_code</field>
        <field name="code">
for line in self:
    if not line.name or not line.name.strip():
        failed = True
        break
        </field>
    </record>

    <record id="pr_line_exception_price_unit_min_0" model="exception.rule">
        <field name="name">ราคาต่อหน่วยต้องมากกว่า 0</field>
        <field name="description">ระบบไม่อนุญาตให้ราคาต่อหน่วยเป็น 0 หรือค่าติดลบ</field>
        <field name="model">purchase.request.line</field>
        <field name="exception_type">by_py_code</field>
        <field name="code">
for line in self:
    if not line.price_unit or line.price_unit &lt;= 0:
        failed = True
        break
        </field>
    </record>
</odoo>