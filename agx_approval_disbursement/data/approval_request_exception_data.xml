<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="excep_no_disbursement_evidence" model="exception.rule">
        <field name="name">There is no evidence of a disbursement request.</field>
        <field name="description">Please provide at least one piece of supporting documentation for your disbursement request.</field>
        <field name="sequence">10</field>
        <field name="model">approval.request</field>
        <field name="is_blocking" eval="True"/>
        <field name="code">
if self.state == 'approved' and not self.disbursement_attachment_ids:
    failed = True
        </field>
        <field name="active" eval="True"/>
    </record>
</odoo>
