<odoo>
    <template id="portal_account_asset_page" name="Asset Portal Page" primary="True">
        <t t-call="portal.portal_layout">
            <!-- Title -->
            <t t-set="title">Asset Information</t>

            <!-- <div class="container mt-4">
                <h2 class="mb-3">รายละเอียดครุภัณฑ์</h2>
                <hr />

                <t t-if="asset">
                    <div class="row mt-3">
                        <div class="col-12 col-md-4 text-center">
                            <img
                                t-if="asset.image"
                                t-att-src="'data:image/png;base64,%s' % asset.image"
                                class="img-thumbnail"
                                style="max-width: 250px"
                            />
                            <div t-if="not asset.image" class="text-muted">
                                ไม่มีรูปภาพ
                            </div>
                        </div>

                        <div class="col-12 col-md-8">
                            <table class="table table-borderless table-sm">
                                <tbody>
                                    <tr>
                                        <th class="text-muted">เลขที่ครุภัณฑ์:</th>
                                        <td><t t-esc="asset.number" /></td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted">ชื่อรายการ:</th>
                                        <td><t t-esc="asset.name" /></td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted">สถานะ:</th>
                                        <td><t t-esc="asset.state" /></td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted">ต้นทุน:</th>
                                        <td><t t-esc="asset.depreciation_base" /></td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted">วันที่เริ่มต้น:</th>
                                        <td><t t-esc="asset.date_start" /></td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted">ประเภทครุภัณฑ์ (Profile):</th>
                                        <td><t t-esc="asset.profile_id.name" /></td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted">GPSC:</th>
                                        <td><t t-esc="asset.gpsc_id.name" /></td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted">หน่วยงาน:</th>
                                        <td><t t-esc="asset.department_id.name" /></td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted">โครงการที่จัดซื้อ (PO):</th>
                                        <td>
                                            <t t-if="asset.purchase_id">
                                                <t t-esc="asset.purchase_id.name" />
                                            </t>
                                            <t t-else="">-</t>
                                        </td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted">สถานที่:</th>
                                        <td><t t-esc="asset.asset_location_id.name" /></td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </t>

                <t t-else="">
                    <div class="alert alert-danger">ไม่พบข้อมูลครุภัณฑ์</div>
                </t>
            </div> -->
        </t>
    </template>
</odoo>
