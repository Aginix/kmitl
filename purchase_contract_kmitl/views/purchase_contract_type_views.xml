<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_purchase_contract_type_tree" model="ir.ui.view">
        <field name="name">view.purchase.contract.type.tree</field>
        <field name="model">purchase.contract.type</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name" />
                <field name="is_construction" optional="hide" />
            </tree>
        </field>
    </record>

    <record id="view_purchase_contract_type_form" model="ir.ui.view">
        <field name="name">purchase.contract.type.form(Contract KMITL)</field>
        <field name="model">purchase.contract.type</field>
        <field name="arch" type="xml">
            <form string="Purchase Contract Type">
                <sheet>
                    <div class="oe_title">
                        <span class="o_form_label">Purchase Contract Type </span>
                        <h1 class="d-flex">
                            <field name="name" />
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="is_construction" />
                            <field name="active" invisible="1" />
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" />
                    <field name="activity_ids" />
                    <field name="message_ids" />
                </div>
            </form>
        </field>
    </record>

    <record id="view_purchase_contract_type_search" model="ir.ui.view">
        <field name="name">view.purchase.contract.type.search</field>
        <field name="model">purchase.contract.type</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" name="group_by" string="Group By">
                    <filter
                        string="Name"
                        name="name"
                        domain="[]"
                        context="{'group_by':'name'}"
                    />
                </group>
            </search>
        </field>
    </record>

    <record id="action_purchase_contract_type" model="ir.actions.act_window">
        <field name="name">Purchase Contract Type</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">purchase.contract.type</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Purchase Contract Type.
            </p>
        </field>
    </record>

    <menuitem
        id="menu_purchase_contract_type_kmitl"
        parent="purchase.menu_purchase_config"
        action="action_purchase_contract_type"
    />
</odoo>
