<odoo>
    <record id="purchase_order_contract_name_exception" model="exception.rule">
        <field name="name">ต้องระบุชื่อสัญญา</field>
        <field name="description">ข้อมูลใบสั่งซื้อ/จ้าง/สัญญา</field>
        <field name="model">purchase.order</field>
        <field name="exception_type">by_py_code</field>
        <field name="code">
if not self.contract_name:
    failed = True
        </field>
    </record>

    <record id="purchase_order_contract_type_exception" model="exception.rule">
        <field name="name">ต้องระบุประเภทสัญญา</field>
        <field name="description">ข้อมูลใบสั่งซื้อ/จ้าง/สัญญา</field>
        <field name="model">purchase.order</field>
        <field name="exception_type">by_py_code</field>
        <field name="code">
if not self.contract_type_id:
    failed = True
        </field>
    </record>

    <record id="purchase_order_fines_rate_exception" model="exception.rule">
        <field name="name">ต้องระบุค่าปรับต่อวัน</field>
        <field name="description">ข้อมูลใบสั่งซื้อ/จ้าง/สัญญา</field>
        <field name="model">purchase.order</field>
        <field name="exception_type">by_py_code</field>
        <field name="code">
if self.fines_rate &lt; 0:
    failed = True
        </field>
    </record>

    <record id="purchase_order_work_start_must_be_valid_exception" model="exception.rule">
        <field name="name">วันที่เริ่มงาน ต้องไม่เริ่มก่อนวันที่เริ่มสัญญา</field>
        <field name="description">ประเภทสัญญางานก่อสร้าง</field>
        <field name="model">purchase.order</field>
        <field name="exception_type">by_py_code</field>
        <field name="code">
if self.work_start &lt; self.date_order_date:
    failed = True
        </field>
    </record>

    <record id="purchase_order_work_start_must_be_valid_exception" model="exception.rule">
        <field name="name">วันที่เริ่มงาน ต้องไม่เริ่มก่อนวันที่เริ่มสัญญา</field>
        <field name="description">ประเภทสัญญางานก่อสร้าง</field>
        <field name="model">purchase.order</field>
        <field name="exception_type">by_py_code</field>
        <field name="code">
if self.work_start &lt; self.date_order_date:
    failed = True
        </field>
    </record>

    <record id="purchase_order_contract_name_requried_exception" model="exception.rule">
        <field name="name">จำเป็นต้องมีชื่อสัญญา/ใบสั่งซื้อ/จ้าง</field>
        <field name="description">ประเภทสัญญางานก่อสร้าง</field>
        <field name="model">purchase.order</field>
        <field name="exception_type">by_py_code</field>
        <field name="code">
if not self.contract_name:
    failed = True
        </field>
    </record>

    <record id="purchase_order_contract_number_requried_exception" model="exception.rule">
        <field name="name">จำเป็นต้องมีเลขที่สัญญา</field>
        <field name="description">ประเภทสัญญางานก่อสร้าง</field>
        <field name="model">purchase.order</field>
        <field name="exception_type">by_py_code</field>
        <field name="code">
if not self.contract_number:
    failed = True
        </field>
    </record>
</odoo>
