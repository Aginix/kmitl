<?xml version="1.0" encoding="utf-8" ?>
<odoo noupdate="1">
    <!-- ยอดเงินของเอกสารนี้ เกินยอดเงินในเอกสาร PR -->
    <record id="purchase_exception_amount_not_exceed_pr" model="exception.rule">
        <field name="name">The amount in this document exceeds the amount in the PR document.</field>
        <field name="description">Please check your balance again.</field>
        <field name="model">purchase.order</field>
        <field name="exception_type">by_py_code</field>
        <field name="code">
pr_total = sum(self.order_line.mapped("purchase_request_lines.estimated_cost"))
if pr_total:
    po_total = self.amount_total
    failed = po_total &gt; pr_total
        </field>
    </record>
    <!-- ข้อมูลประเภทการเบิกจ่ายในใบสั่งซื้อ/จ้าง (PO) ต้องตรงกับในใบพ.1 (PR) -->
    <record id="po_payment_type_should_be_the_same_as_pr" model="exception.rule">
        <field name="name"
            >The payment type in Purchase Order (PO) must match the payment type in Purchase Request (PR)</field
        >
        <field name="description">Purchase Order Information</field>
        <field name="model">purchase.order</field>
        <field name="exception_type">by_py_code</field>
        <field name="code">
if self.request_id and self.request_id.payment_type != self.payment_type:
    failed = True
        </field>
    </record>
</odoo>
