<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_stock_request_custom_filter" model="ir.ui.view">
        <field name="name">stock.request.select - stock_request_tier_validation</field>
        <field name="model">stock.request</field>
        <field name="inherit_id" ref="stock_request_aginix.view_stock_request_search" />
        <field name="arch" type="xml">
            <xpath expr="//group" position="inside">
                <filter
                    name="needs_review"
                    string="Needs my Review"
                    domain="[('reviewer_ids','in',uid), ('state', 'not in', ['approved', 'done', 'cancel'])]"
                    help="My Stock Request Orders to review"
                />
                <filter
                    name="tier_validated"
                    string="Validated"
                    domain="[('validation_status', '=', 'validated')]"
                    help="Stock Requests validated and ready to be confirmed"
                />
            </xpath>
        </field>
    </record>
</odoo>
