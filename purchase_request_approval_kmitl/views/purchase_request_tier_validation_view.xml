<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_purchase_request_approval_tier_form" model="ir.ui.view">
        <field name="name">purchase_request_approval_tier_validaition</field>
        <field name="model">purchase.request</field>
        <field name="inherit_id" ref="purchase_request.view_purchase_request_form"/>
        <field name="arch" type="xml">
            <button name="button_draft" position="after">
                <field name="rejected" invisible="1" />
                <field name="validation_status" invisible="1" />
                <field name="need_validation" invisible="1" />
                <field name="can_request" invisible="1" />
                <button name="request_validation" string="Request Validation" class="btn-primary" attrs="{'invisible': ['|', '|', '|', '|',
        ('need_validation', '!=', True),
        ('validation_status', '=', 'pending'),
        ('rejected', '=', True),
        ('substate_sequence', '!=', 30), ('can_request','=',False)
    ]}" type="object" />
                <button name="restart_validation" string="Restart Validation" class="btn-primary" attrs="{'invisible': ['|', '|',
        ('need_validation', '!=', True),
        ('validation_status', '!=', 'pending'), ('can_request','=',False)
    ]}" type="object" groups="purchase_request.group_purchase_request_manager" />
            </button>
        </field>
    </record>
</odoo>
