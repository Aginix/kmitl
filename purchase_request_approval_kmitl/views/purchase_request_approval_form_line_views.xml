<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_purchase_request_approval_form_line_tree" model="ir.ui.view">
        <field name="name">view.purchase.request.approval.form.line.tree</field>
        <field name="model">purchase.request.approval.form.line</field>
        <field name="arch" type="xml">
            <tree>
                <field name="product_id"/>
                <field name="description"/>
            </tree>
        </field>
    </record>

    <record id="view_purchase_request_approval_form_line_form" model="ir.ui.view">
        <field name="name">view.purchase.request.approval.form.line.form</field>
        <field name="model">purchase.request.approval.form.line</field>
        <field name="arch" type="xml">
            <form string="Purchase Request Approval Form Line">
                <sheet>
                    <group>
                        <group>
                            <field name="product_id" />
                            <field name="description"/>
                        </group>
                        <group>
                            <field name="quantity"/>
                            <field name="uom"/>
                            <field name="unit_price"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action purchase.request.two.line -->
    <record id="action_purchase_request_approval_form_line" model="ir.actions.act_window">
        <field name="name">Purchase Request Approval Form Line</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">purchase.request.approval.form.line</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Purchase Request Two Line.
            </p>
        </field>
    </record>

</odoo>
