<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_purchase_request_form" model="ir.ui.view">
        <field name="name">purchase_request_approval_kmitl_form</field>
        <field name="model">purchase.request</field>
        <field name="inherit_id" ref="purchase_request.view_purchase_request_form"/>
        <field name="arch" type="xml">
            <field name="state" position="attributes">
                <attribute name="statusbar_visible">draft,to_verify,to_approve,approved,done,in_progress</attribute>
            </field>
            <button name="button_to_approve" position="after">
                <button name="button_to_verify" states="draft" string="Request approval" type="object" class="oe_highlight"/>
            </button>
            <button name="button_approved" position="attributes">
                <attribute name="invisible">1</attribute>
            </button>
            <!-- substate btn -->
            <button name="button_draft" position="before">
                <field name="substate_sequence" invisible="1" />
                <field name="can_edit_budget" invisible="1" />
                <button id="button_substate_verify" name="action_to_substate" type="object" string="Verify" context="{'to_substate_sequence': 30}" attrs="{'invisible': ['|', ('substate_sequence', '!=', 20), ('state', '!=', 'to_verify')]}" groups="purchase_request_security.group_purchase_request_user_all" class="oe_highlight" />
            </button>
            <!-- budget -->
            <xpath expr="//button[@name='action_reserve_budget']" position="attributes">
                <attribute name="groups">purchase_request_approval_kmitl.group_purchase_request_budget</attribute>
                <attribute name="attrs">{'invisible':['|', ('state','!=','to_verify'), ('substate_sequence', '!=', 10)]}</attribute>
            </xpath>
            <field name="budget_account_id" position="attributes">
                <attribute name="attrs">{'readonly':[('can_edit_budget','=',False)]}</attribute>
            </field>
            <field name="activity_analytic_id" position="attributes">
                <attribute name="attrs">{'readonly':[('can_edit_budget','=',False)]}</attribute>
            </field>
            <field name="department_analytic_id" position="attributes">
                <attribute name="attrs">{'readonly':[('can_edit_budget','=',False)]}</attribute>
            </field>
            <field name="fund_analytic_id" position="attributes">
                <attribute name="attrs">{'readonly':[('can_edit_budget','=',False)]}</attribute>
            </field>
            <field name="source_analytic_id" position="attributes">
                <attribute name="attrs">{'readonly':[('can_edit_budget','=',False)]}</attribute>
            </field>
            <field name="substate_id" position="attributes">
                <attribute name="attrs">{'invisible':[('state','!=','to_verify')]}</attribute>
            </field>
        </field>
    </record>
</odoo>
