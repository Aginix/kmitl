<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- No view overrides needed — field locking is handled by
         is_locked_by_reference computed field in the base module,
         with _is_locked_by_reference() overridden in this bridge module's
         advance_payment.py model. -->
</odoo>
