<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Form view: add DR smart button (navigate to the linked Disbursement Request) -->
    <record id="view_account_move_form_inherit_disbursement" model="ir.ui.view">
        <field name="name">account.move.form.inherit.disbursement_accounting_kmitl</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_disbursement_request"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-file-text-o"
                        groups="accounting_kmitl.group_accounting_kmitl_user"
                        attrs="{'invisible': [('disbursement_request_name', '=', False)]}">
                    <div class="o_field_widget o_stat_info">
                        <span class="o_stat_text">Disbursement Request</span>
                        <span class="o_stat_text"><field name="disbursement_request_name" nolabel="1"/></span>
                    </div>
                </button>
            </xpath>
            <!-- Show the budget already consumed from the linked Disbursement Request -->
            <xpath expr="//group[@name='budget_info']" position="after">
                <group name="disbursement_budget" string="Budget Consumed from DR"
                       attrs="{'invisible': [('disbursement_request_id', '=', False)]}">
                    <field name="disbursement_request_id" invisible="1"/>
                    <field name="disbursement_budget_consumed_amount"/>
                    <field name="disbursement_budget_consumed_date"/>
                    <field name="disbursement_budget_commitment_id"/>
                </group>
            </xpath>
        </field>
    </record>

    <!-- Primary-inherit tree view for DR "รายการบันทึกบัญชี" smart button.
         Same arch as account.view_move_tree; owned as a distinct view id so
         the DR action can bind it alongside the search panel below without
         touching the stock tree view. -->
    <record id="view_move_tree_disbursement_related" model="ir.ui.view">
        <field name="name">account.move.tree.disbursement.related</field>
        <field name="model">account.move</field>
        <field name="mode">primary</field>
        <field name="inherit_id" ref="account.view_move_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="attributes">
                <attribute name="string">รายการบันทึกบัญชี</attribute>
            </xpath>
        </field>
    </record>

    <!-- Primary-inherit search view adding a journal_id search panel.
         The stock account.view_account_move_filter is left untouched. -->
    <record id="view_account_move_filter_disbursement_related" model="ir.ui.view">
        <field name="name">account.move.select.disbursement.related</field>
        <field name="model">account.move</field>
        <field name="mode">primary</field>
        <field name="inherit_id" ref="account.view_account_move_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//search" position="inside">
                <searchpanel>
                    <field name="journal_id" select="one" icon="fa-book"/>
                </searchpanel>
            </xpath>
        </field>
    </record>

    <!-- Action opened by the DR "รายการบันทึกบัญชี" smart button. Binds the
         new primary tree view + search view (with journal_id search panel);
         form falls back to the stock account.view_move_form. -->
    <record id="action_disbursement_related_moves" model="ir.actions.act_window">
        <field name="name">รายการบันทึกบัญชี</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="view_account_move_filter_disbursement_related"/>
    </record>

    <record id="action_disbursement_related_moves_tree" model="ir.actions.act_window.view">
        <field name="sequence" eval="1"/>
        <field name="view_mode">tree</field>
        <field name="view_id" ref="view_move_tree_disbursement_related"/>
        <field name="act_window_id" ref="action_disbursement_related_moves"/>
    </record>

    <record id="action_disbursement_related_moves_form" model="ir.actions.act_window.view">
        <field name="sequence" eval="2"/>
        <field name="view_mode">form</field>
        <field name="view_id" ref="account.view_move_form"/>
        <field name="act_window_id" ref="action_disbursement_related_moves"/>
    </record>

</odoo>
