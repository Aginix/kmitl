<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="budget_selection_wizard_form" model="ir.ui.view">
        <field name="name">budget.selection.wizard.form</field>
        <field name="model">budget.selection.wizard</field>
        <field name="arch" type="xml">
            <form string="เลือกงบประมาณ">
                <group>
                    <field name="res_model" invisible="1"/>
                    <field name="res_id" invisible="1"/>
                    <field name="budget_account_id"
                           options="{'no_create_edit': True, 'no_open': True, 'no_create': True}"/>
                    <field name="activity_analytic_id"
                           options="{'no_create_edit': True, 'no_open': True, 'no_create': True}"/>
                    <field name="department_analytic_id"
                           options="{'no_create_edit': True, 'no_open': True, 'no_create': True}"/>
                    <field name="fund_analytic_id" widget="selection"/>
                    <field name="source_analytic_id" widget="selection"/>
                </group>
                <footer>
                    <button string="บันทึก" name="action_apply" type="object" class="btn-primary"/>
                    <button string="ยกเลิก" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_budget_selection_wizard" model="ir.actions.act_window">
        <field name="name">เลือกงบประมาณ</field>
        <field name="res_model">budget.selection.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
