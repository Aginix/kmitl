<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <data noupdate="1">
        <!--
            Per-(dept, fiscal year) sequences for receipt.kmitl and cash deposit
            are created lazily by the model's _get_receipt_sequence /
            _get_sequence methods at first use.

            This file is kept as a placeholder if any non-lazy sequence needs to
            be added later.
        -->
    </data>
</odoo>
