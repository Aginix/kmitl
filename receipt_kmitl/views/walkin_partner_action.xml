<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="action_receipt_kmitl_walkin_partner" model="ir.actions.server">
        <field name="name">ลูกค้า Walk-In</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="state">code</field>
        <field name="code">
walkin_id = env["kmitl.receipt"]._default_partner_id()
if not walkin_id:
    raise UserError("ยังไม่ได้ตั้งค่าลูกค้า Walk-In")
action = {
    "type": "ir.actions.act_window",
    "res_model": "res.partner",
    "res_id": walkin_id,
    "view_mode": "form",
    "target": "current",
}
        </field>
    </record>
</odoo>
