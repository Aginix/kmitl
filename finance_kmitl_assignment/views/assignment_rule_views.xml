<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_finance_assignment_rule_tree" model="ir.ui.view">
        <field name="name">finance.assignment.rule.tree</field>
        <field name="model">finance.assignment.rule</field>
        <field name="arch" type="xml">
            <tree string="Assignment Rules" editable="bottom">
                <field name="sequence" widget="handle" />
                <field name="department_analytic_id" options="{'no_create': True}" />
                <field name="source_analytic_id" options="{'no_create': True}" />
                <field name="fund_analytic_id" options="{'no_create': True}" />
                <field name="activity_analytic_id" options="{'no_create': True}" />
                <field name="partner_type_id" options="{'no_create': True}" />
                <field
                    name="paying_account_id"
                    options="{'no_create': True, 'no_open': True}"
                />
                <field
                    name="payment_subject_id"
                    options="{'no_create': True, 'no_open': True}"
                />
                <field name="user_id" options="{'no_create': True}" />
                <field name="active" widget="boolean_toggle" />
            </tree>
        </field>
    </record>

    <record id="action_finance_assignment_rule" model="ir.actions.act_window">
        <field name="name">Assignment Rules</field>
        <field name="res_model">finance.assignment.rule</field>
        <field name="view_mode">tree</field>
        <field name="context">{'active_test': False}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                สร้างกฎเพื่อมอบหมายใบสำคัญจ่ายให้เจ้าหน้าที่การเงินผู้รับผิดชอบ
            </p>
            <p>
                เว้นเงื่อนไขว่างไว้เพื่อให้เป็น wildcard (จับคู่ได้ทุกค่า) กฎจะถูกจับคู่จากบนลงล่าง
                โดยกฎแรกที่ตรงเป็นผู้ชนะ กฎที่ตั้งบนส่วนงาน/กองทุน/กิจกรรมระดับแม่
                จะครอบคลุมระดับลูกทั้งหมด
            </p>
            <p>
                เรื่องที่จ่ายมีเฉพาะใบสำคัญจ่ายที่สร้างจากใบเบิกเงิน
                ใบที่คีย์เองจะเข้าได้เฉพาะกฎที่เว้นเงื่อนไขนี้ว่างไว้
            </p>
        </field>
    </record>

    <!-- Gear-menu action to route already-created vouchers after editing rules -->
    <record id="action_apply_rules_to_pending" model="ir.actions.server">
        <field name="name">Apply Rules to Pending Payments</field>
        <field name="model_id" ref="model_finance_assignment_rule" />
        <field name="binding_model_id" ref="model_finance_assignment_rule" />
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = model.action_apply_to_pending()</field>
    </record>

    <menuitem
        id="menu_finance_assignment_rule"
        name="Assignment Rules"
        parent="finance_kmitl.menu_settings_payment_data"
        action="action_finance_assignment_rule"
        sequence="40"
    />
</odoo>
