<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View purchase.order form -->
    <record id="view_purchase_order_form" model="ir.ui.view">
        <field name="name">view.purchase.order.form</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_reserve_budget" string="Reserve budget" type="object" attrs="{'invisible': [('budget_commitment_id', '!=', False)]}" />
            </xpath>
            <xpath expr="//sheet/group" position="after">
                <group string="Budgeting" name="budgeting">
                    <field name="activity_analytic_id" />
                    <field name="department_analytic_id" />
                    <field name="fund_analytic_id" />
                    <field name="source_analytic_id" />
                    <field name="budget_account_id" />
                    <field name="budget_commitment_id" readonly="1" />
                </group>
            </xpath>
        </field>
    </record>

</odoo>
