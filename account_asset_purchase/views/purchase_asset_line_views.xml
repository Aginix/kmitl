<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View purchase.asset.line View List -->
    <record id="view_purchase_asset_line_list" model="ir.ui.view">
        <field name="name">view.purchase.asset.line.list</field>
        <field name="model">purchase.asset.line</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- View purchase.asset.line form -->
    <record id="view_purchase_asset_line_form" model="ir.ui.view">
        <field name="name">view.purchase.asset.line.form</field>
        <field name="model">purchase.asset.line</field>
        <field name="arch" type="xml">
            <form string="Purchase Asset Line">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- View purchase.asset.line search -->
    <record id="view_purchase_asset_line_search" model="ir.ui.view">
        <field name="name">view.purchase.asset.line.search</field>
        <field name="model">purchase.asset.line</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action purchase.asset.line -->
    <record id="action_purchase_asset_line" model="ir.actions.act_window">
        <field name="name">Purchase Asset Line</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">purchase.asset.line</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Purchase Asset Line.
            </p>
        </field>
    </record>

</odoo>
